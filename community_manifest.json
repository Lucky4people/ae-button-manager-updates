{
  "buttons": [
    {
      "name": "Create Null",
      "description": "Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition. Creates a new Null Object layer in the active composition.",
      "script": "var comp = app.project.activeItem; if (comp && comp instanceof CompItem) { comp.layers.addNull(); } else { alert('Please select a composition.'); }"
    },
    {
      "name": "Toggle Shy Layers",
      "description": "Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy l ayers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp. Shows or hides all shy layers in the active comp.",
      "script": "var comp = app.project.activeItem; if (comp && comp instanceof CompItem) { comp.hideShyLayers = !comp.hideShyLayers; }"
    },
    {
      "name": "Center Anchor Point",
      "description": "Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers. Centers the anchor point for all selected layers.",
      "script": "var selectedLayers = app.project.activeItem.selectedLayers; if (selectedLayers.length > 0) { app.executeCommand(app.findMenuCommandId('Center Anchor Point in Layer Content')); } else { alert('Please select at least one layer.'); }"
    }
  ]
}
